
export default function Header() {
    return (
        <header className={"fixed top-0 w-screen flex items-center bg-white h-20 pl-5 z-1"}>
            <h1 className={"text-4xl font-semibold"}>OAuth App</h1>
        </header>
    );
}